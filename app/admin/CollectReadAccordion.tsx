'use client';
import React from 'react';
// import * as z from 'zod';

export default function CollectReadAccordion() {
  // const collectListFormSchema = z.object({
  //   ipOrgId: z.string().min(1, {
  //     message: 'Required.',
  //   }),
  // });

  return (
    <div className="flex flex-col gap-2">
      {/* <h1 className="font-semibold text-2xl">Collect</h1>

      <AccordionInputForm fcnName={'collect.list'} formSchema={collectListFormSchema} onSubmitFcn={listCollection} /> */}
    </div>
  );
}
